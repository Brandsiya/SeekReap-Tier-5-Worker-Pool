app = "seekreap-tier5-worker"
primary_region = "jnb"

[build]
  dockerfile = "Dockerfile"

[env]

[http_service]
  internal_port = 8080
  force_https = false
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
