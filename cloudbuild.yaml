# Tier-5 Worker Pool - local worker scripts
# seekreap-worker Cloud Run service is deployed via SeekReap-Tier-6-Backend trigger
steps:
  - name: 'alpine'
    args: ['echo', 'Tier-5 Worker Pool - no Cloud Run deployment needed']
options:
  logging: CLOUD_LOGGING_ONLY
